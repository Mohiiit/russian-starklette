import React, { useEffect, useState } from 'react';
import {
  Button,
  Dialog,
  DialogActions,
  DialogContent,
  DialogContentText,
  DialogTitle,
  Typography,
  Box,
  TextField, Backdrop
} from '@mui/material';
import { useGame } from '../context/ProviderContext';
import { createGameFactoryContract, getBalance, placeBet, updateAmount, updateNumber } from '../utils';

function BetStatusComponent({}) {


    return (
        <></>
    );
}

export default BetStatusComponent;